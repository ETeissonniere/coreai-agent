# Core AI model bundles. Git attributes do not recursively match directories,
# so track both bundle contents and the compiled payload extension explicitly.
*.aiengine/** filter=lfs diff=lfs merge=lfs -text
*.aimodel/** filter=lfs diff=lfs merge=lfs -text
*.mlirb filter=lfs diff=lfs merge=lfs -text
